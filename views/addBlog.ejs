<!-- <!DOCTYPE html>
<html lang="en">
<head>
    
    <title>Blogify | Add Blog</title>
</head>
<body>
    

<div class="container mt-3">
    <form action="/blog" method="post" enctype="multipart/form-data">
        <div class='mb-3'>
            <label for="coverImage" class="form-label">Cover Image</label>
            <input 
                type="file" 
                class="form-control" 
                id="coverImage" 
                name="coverImage" 
                aria-describedby="coverImage"/>
        </div>
        <div class='mb-3'>
            <label for="title" class="form-label">Title</label>
            <input 
                type="text" 
                class="form-control" 
                id="title" 
                name="title" 
                aria-describedby="title"/>
        </div>
        <div class='mb-3'>
            <label for="body">Body</label>
            <textarea name="body" class="form-control" id="body"></textarea>
        </div>
        <div class="mb-3">
            <button class="btn btn-primary">Submit</button>
        </div>
    </form>
</div>
 
</body>
</html> -->

<!-- <!DOCTYPE html>
<html lang="en">
<head>
    <%-include('./partials/head') %>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/styles.css">
    <title>Blogify | Add Blog</title>
</head>
<body>
    <%- include('./partials/nav') %>
    <header>
        <h1>Add New Blog</h1>
    </header>
    <div class="container">
        <form action="/blog" method="POST" enctype="multipart/form-data">
            <div class="mb-3">
                <label for="coverImage" class="form-label">Cover Image</label>
                <div class="custom-file">
                    <input 
                        type="file" 
                        class="custom-file-input" 
                        id="coverImage" 
                        name="coverImage" 
                        aria-describedby="coverImage" 
                        accept="image/*" 
                        onchange="updateFileName()"/>
                    <label class="custom-file-label" for="coverImage">Choose file...</label>
                </div>
                <small class="form-text text-muted">Upload a cover image (JPEG, PNG, etc.)</small>
            </div>
            <input type="text" name="title" placeholder="Blog Title" required>
            <textarea name="body" placeholder="Blog Content" required></textarea>
            <input type="submit" value="Add Blog">
        </form>
    </div>
    <script>
        function updateFileName() {
            const input = document.getElementById('coverImage');
            const label = document.querySelector('.custom-file-label');
            const fileName = input.files[0] ? input.files[0].name : 'Choose file...';
            label.textContent = fileName;
            label.classList.add('selected');
        }
        </script>
    <%-include('./partials/scripts') %>
</body>
</html> -->


<!DOCTYPE html>
<html lang="en">
<head>
    <%-include('./partials/head') %>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Blogify | Add Blog</title>
    <style>
        body {
            background-color: #141414;
            color: white;
            font-family: 'Helvetica Neue', sans-serif;
            margin: 0;
            padding: 0;
        }

        header {
            text-align: center;
            padding: 2rem;
            background-color: #000;
            font-size: 2rem;
            font-weight: bold;
            border-bottom: 1px solid #333;
        }

        .container {
            max-width: 700px;
            margin: 40px auto;
            background-color: #1f1f1f;
            padding: 2rem;
            border-radius: 10px;
            box-shadow: 0 0 15px rgba(255, 0, 0, 0.1);
        }

        form input[type="text"],
        form textarea {
            width: 100%;
            padding: 12px;
            margin: 15px 0;
            background-color: #333;
            border: none;
            border-radius: 5px;
            color: white;
            font-size: 1rem;
        }

        input[type="submit"] {
            background-color: #e50914;
            color: white;
            border: none;
            padding: 12px 20px;
            cursor: pointer;
            font-size: 1rem;
            border-radius: 5px;
            transition: 0.3s ease;
        }

        input[type="submit"]:hover {
            background-color: #f40612;
        }

        label, small {
            color: #bbb;
        }
    </style>
</head>
<body>
    <%- include('./partials/nav') %>
    <header>Add New Blog</header>
    <div class="container">
        <form action="/blog" method="POST" enctype="multipart/form-data">
            <div>
                <label for="coverImage">Cover Image</label><br>
                <input type="file" id="coverImage" name="coverImage" accept="image/*" onchange="updateFileName()"/>
                <small>Upload a cover image (JPEG, PNG, etc.)</small>
            </div>
            <input type="text" name="title" placeholder="Blog Title" required>
            <textarea name="body" placeholder="Blog Content" required rows="8"></textarea>
            <input type="submit" value="Add Blog">
        </form>
    </div>
    <script>
        function updateFileName() {
            const input = document.getElementById('coverImage');
            const fileName = input.files[0]?.name || 'Choose file...';
            alert("Selected File: " + fileName);
        }
    </script>
    <%-include('./partials/scripts') %>
</body>
</html>
